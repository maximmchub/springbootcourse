package ua.raif.courses.domain;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.time.LocalDate;

import static org.junit.jupiter.api.Assertions.*;

class ConferenceTest {

/*    @Ignore @Test
    void create() {
        Talk talk = new Talk();
        talk.setCaption("Seabed reflection in salt water.");
        talk.setDescription("instructive experience");
        talk.setSpeaker("Bootstrap Bill Turner");
        talk.setType(TalkType.LECTURE);

        Conference conf = new Conference();
        conf.setCapacity(300);
        conf.setCaption("Problems of hydraacustics");
        conf.setDates(new ConferenceDates(LocalDate.of(2031, 9, 20), LocalDate.of(2031, 9, 21)));
        conf.setDescription("All marine noobs concentrated here...");
        conf.addTalk(talk);

        System.out.println(conf.toString());
    }*/
}